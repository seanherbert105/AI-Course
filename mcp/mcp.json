{
  "mcpServers": {
    "eval": {
      "command": "docker",
      "args": [
        "run",
        "--network=ai-course_ai",
        "--name=mcp-eval",
        "-i",
        "--rm",
        "-e", "WEAVIATE_FIELDS=filename,content",
        "mcp/eval:latest"
      ]
    }
  }
}
